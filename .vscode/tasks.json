
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Compile and Run with I/O",
            "command": "${workspaceFolder}/runjava.sh",
            "args": [
                "${file}",
                "${workspaceFolder}",
                "${workspaceFolder}/input.txt",
                "${workspaceFolder}/output.txt"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
  }
